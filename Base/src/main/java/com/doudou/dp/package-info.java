/**
 * 此包主要用来记录设计模式的学习笔记。
 * 23中常用设计模式的分类如下：
 * 创建型：
 * 行为型:
 * 结构型：
 *
 * 设计模式的六大原则：
 *
 * 开闭原则：
 *     对扩展开放，对修改闭合
 * 依赖反转：
 *     抽象不依赖细节，细节依赖抽象
 * 里氏替换：
 *     凡是用父类的地方均可以用子类替换
 * 迪米特法则：
 *     一个软件实体应该尽可能少的与其他实体发生
 *     相互作用（用来降低类与类之间的耦合，只和
 *     直接朋友讲话，不和陌生人讲话）
 * 职责单一：
 *     一个类只负责一个功能领域的相应职责，即就
 *     一个类而言，只应有一个原因能引起它的变化。
 *     （实现高内聚低耦合的指导方针）
 * 接口隔离：
 *     客户端不应该依赖它不需要的接口，类间的关
 *     系应该建立在最小接口上。（最少知道原则）
 */
package com.doudou.dp;
